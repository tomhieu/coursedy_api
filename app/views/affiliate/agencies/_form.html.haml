= form_for @affiliate_agency do |f|
  - if @affiliate_agency.errors.any?
    #error_explanation
      %h2= "#{pluralize(@affiliate_agency.errors.count, "error")} prohibited this affiliate_agency from being saved:"
      %ul
        - @affiliate_agency.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :address
    = f.text_field :address
  .field
    = f.label :phone
    = f.text_field :phone
  .field
    = f.label :domain
    = f.text_field :domain
  .field
    = f.label :user
    = f.text_field :user
  .actions
    = f.submit 'Save'
